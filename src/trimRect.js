//from a point in rect insode ploygon to a poiny ourside polygon push intersection to line atray. 
// push all points in pilygon regardless

//bathroom above the bench
//let verticesArray = [0, 0, 839, 0, 848, 1860, 0, 1854];
export function trimRect(rect) {
  const pointsOfRect = rect.points;
  return pointsOfRect;
}
